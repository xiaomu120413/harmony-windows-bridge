import { RdpPermissionManager } from './RdpPermissions';
import { XrdpServerDisplayMessages, XrdpServerStatus } from './XrdpServerController';
export interface RemoteControlSnapshot {
    accessCode: string;
    accessCodeGateEnabled: boolean;
    screenRecordingPermissionGranted: boolean;
    screenRecordingPermissionBusy: boolean;
    inputInjectionPermissionGranted: boolean;
    inputInjectionPermissionBusy: boolean;
    serverRunning: boolean;
    serverState: string;
    serverPort: number;
    serverMessage: string;
    serverBusy: boolean;
}
export declare class RemoteControlCoordinator {
    private readonly available;
    private readonly permissionManager;
    private readonly appFilesDir;
    private readonly onChange;
    private readonly serverController;
    private startRequest;
    private accessCode;
    private accessCodeGateEnabled;
    private accessCodeInitialized;
    private permissionGranted;
    private permissionBusy;
    private inputInjectionPermissionGranted;
    private inputInjectionPermissionBusy;
    private serverRunning;
    private serverState;
    private serverPort;
    private serverMessage;
    private serverBusy;
    constructor(available: boolean, messages: XrdpServerDisplayMessages, permissionManager: () => RdpPermissionManager, appFilesDir: () => string, onChange: (snapshot: RemoteControlSnapshot) => void);
    snapshot(): RemoteControlSnapshot;
    permissionPromptOpened(): void;
    refreshPermissionState(): boolean;
    refreshInputInjectionPermissionState(): boolean;
    requestInputInjectionPermissionFromSettings(): Promise<boolean>;
    refreshDiagnostics(): XrdpServerStatus;
    refreshReadiness(clearBusy?: boolean): XrdpServerStatus;
    requestPermissionFromSettings(): Promise<boolean>;
    startFromSettings(): Promise<XrdpServerStatus>;
    setAccessCodeGate(enabled: boolean): string;
    regenerateAccessCode(): string;
    ensureStarted(trigger: string, restartIfRunning?: boolean, requestPermissionImmediately?: boolean): Promise<XrdpServerStatus>;
    private requestPermissionForRunningStart;
    private hasPermission;
    private applyPermissionResult;
    private startAfterPermissions;
    private startOptions;
    private ensureAccessCode;
    private applyStatus;
    private setPermissionBusy;
    private clearServerBusy;
    private publish;
    private static generateAccessCode;
}
