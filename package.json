{
  "name": "freerdp-control-demo",
  "version": "1.0.1",
  "private": true,
  "description": "Local app demo for connecting to Windows machines through FreeRDP.",
  "scripts": {
    "start": "node app/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
